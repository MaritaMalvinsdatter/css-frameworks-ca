// import { postTemplatePosts } from "/js/templates/post.mjs";
// import * as postMethods from "/js/posts/postIndex.mjs";
// import { getMutiplePosts } from "/js/posts/get.mjs";
// import { getPosts } from "/js/posts/get.mjs";



// export function filterPostsListener(post) {
//     const oldToNew = document.querySelector("#postOldNew");
//     const withImages = document.querySelector("#postImages");
  
    // Shows oldest to newest posts
    // oldToNew.addEventListener("click", (event) => {
    //   const oldestToNewest = post.sort((a, b) => {
    //     console.log(post);
    //     return new Date(a.updated) - new Date(b.updated)
    //   });
    //   getPosts(oldestToNewest);
    // });

    // console.log(post);


    // withImages.addEventListener("click", (event) => {
    //     const postsWithImage = postData.filter((post) => {
    //       if (postData.media === null || postData.media === "") {
    //         return false;
    //       } else {
    //         return true;
    //       }
    //     });
    //     getMutiplePosts(postsWithImage);
    //   });

    // }



